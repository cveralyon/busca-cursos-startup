apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cursosapp.fullname" . }}-test-connection"
  labels:
    {{- include "cursosapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cursosapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
