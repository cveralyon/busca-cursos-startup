apiVersion: v1
kind: Service
metadata:
  name: {{ include "cursosapp.fullname" . }}
  labels:
    {{- include "cursosapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cursosapp.selectorLabels" . | nindent 4 }}
